--- 
 - name: Transfer and execute a script 
   hosts: localhost
   vars: 
     - queue: ""
     - items: "" 
   tasks: 
     - name: Execute script
       shell: /etc/ansible/lab/repro_colas.sh {{queue}} {{items}}
