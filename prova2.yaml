---
 - name: Prueba
   hosts: localhost
   remote_user: guest
   become: no
   vars_prompt: 
     - name: word
       prompt: who? 
       private: false
   tasks:
     - name: start sct 
       shell: /etc/ansible/hi.sh {{word}}

