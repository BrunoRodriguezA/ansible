---
 - name: SCT
   hosts: localhost
   remote_user: guest
   become: no
   vars:
     - status: ""
   tasks:
     - name: START/STOP SCT
       shell: /etc/ansible/hi.sh {{status}}
     - debug:
         msg: "{{status}} cargas SCT"
